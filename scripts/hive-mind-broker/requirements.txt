# Pinned for reproducibility + vulnerability scanning.
# pytest is a TEST-only dep — installed separately in CI (.github/workflows/ci.yml),
# kept out of the app's own dependency set.
fastapi==0.138.0
# Pinned transitively via fastapi; held at the pip-audit-fixed release for
# CVE-2026-48817/48818/54282/54283 (starlette <1.3.1). Bump fastapi in lockstep.
starlette==1.3.1
uvicorn==0.34.0
pyyaml==6.0.2
asyncssh==2.18.0
httpx==0.28.1
